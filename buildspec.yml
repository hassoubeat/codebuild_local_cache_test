version: 0.2

phases:
  install:
    runtime-versions:
      docker: 18
    commands:
    finally:
  pre_build:
    commands:
    finally:
  build:
    commands:
      - docker build .
    finally:
  post_build:
    commands:
    finally:
artifacts:
  files:
    - '**/*'
  name: artifact
  discard-paths: no

